import ProductSpecification, {
  Props,
} from "$store/components/product/ProductSpecification.tsx";

function ProductSpecificationSection(props: Props) {
  return <ProductSpecification {...props} />;
}

export default ProductSpecificationSection;
