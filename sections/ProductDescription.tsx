import ProductDescription, {
  Props,
} from "$store/components/product/ProductDescription.tsx";

function ProductDescriptionSection(props: Props) {
  return <ProductDescription {...props} />;
}

export default ProductDescriptionSection;
