import RichText from "$store/components/ui/RichText.tsx";
import type { Props } from "$store/components/ui/RichText.tsx";

function RichTextSection(props: Props) {
  return <RichText {...props} />;
}

export default RichTextSection;
